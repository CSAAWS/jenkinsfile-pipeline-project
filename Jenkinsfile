pipeline {
    agent any
    stages {
        stage('run') {
            steps {
                echo 'Selma, you can do this!'
                sh 'python3 --version'
                sh 'python3 pipeline.py'
            }
        }
    }
}
